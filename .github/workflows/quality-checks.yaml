---

name: 👌 Quality checks

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  checks:
    runs-on: ${{ matrix.os }}
    name: PHP ${{ matrix.os }}-${{ matrix.php }}
    strategy:
      fail-fast: true
      matrix:
        os:
          - ubuntu-latest
        php:
          - "8.4"

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install PHP with extensions
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: pcov
          tools: composer:v2

      - name: Install Composer dependencies
        uses: ramsey/composer-install@v3

      - name: Run PHPCS
        run: composer cs

      - name: Run PHPStan
        run: composer stat-analyze

      - name: Run tests with coverage
        run: vendor/bin/phpunit --coverage-clover=coverage.xml

      - name: Upload coverage to Coveralls
        uses: coverallsapp/github-action@v2
        with:
          file: coverage.xml
          format: clover
          fail-on-error: true

  mutation-testing:
    runs-on: ubuntu-latest
    name: Mutation testing
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install PHP with extensions
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          coverage: pcov
          tools: composer:v2

      - name: Install Composer dependencies
        uses: ramsey/composer-install@v3

      - name: Run Infection
        run: vendor/bin/infection --threads=max --min-msi=40 --min-covered-msi=40 --logger-github --no-progress

      - name: Upload Infection report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: infection-report
          path: var/infection.*
          if-no-files-found: ignore
